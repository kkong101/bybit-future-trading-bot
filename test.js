const { getAxios, postAxios } = require("./http/index");
const { setBBInfo } = require("./indicator/index");

const ewfwef = () => {
  for (const t of 6) {
    console.log(t);
  }
};

console.log(ewfwef());
